# Attendance-Bot
Uses these commands:  /start → bot replies “type Uses these commands:  /start → bot replies “type 1”  /count → bot replies “type 2”  /total → bot replies “type 3”  /clear → bot replies “type 0”1”  /count → bot replies “type 2”  /total → bot replies “type 3”  /clear → bot replies “type 0”
